{% extends '_template.html.twig' %}

  {% block title %}
      Home
  {% endblock %}

  {% block content %}

      <div class="section">
          <h2 class="title">{{ taskType is defined ? taskType:"" }} Tasks </h2>
      </div>

      {# Affichage des messages flash #}
      {% include 'includes/flash_message.html.twig' %}
      {% if task_table|length > 0 %}
          {% include 'task/table_tasks.html.twig' with task_table %}
      {% else %}
          <div class="notification is-info">
              No task found ...
          </div>
      {% endif %}



  {% endblock %}
