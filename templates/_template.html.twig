{% extends 'base.html.twig' %}
{% block body %}
{#	<nav class="navbar has-background-grey-darker is-dark">#}
	<nav class="navbar is-warning">
		<div class="navbar-brand">
			<a class="navbar-item" href="{{ path('home_page')}}">
				<p class="subtitle is-4 has-text-primary">
					<i class="fas fa-tasks"></i>
					<b>TODO</b>
				</p>
			</a>
			<a class="navbar-item" href="{{ path('overdue_page')}}">
				<p class="subtitle is-4 has-text-primary">
					<i class="fas fa-tasks"></i>
					<b>Overdue Tasks</b>
				</p>
			</a>
			<a class="navbar-item" href="{{ path('due_page')}}">
				<p class="subtitle is-4 has-text-primary">
					<i class="fas fa-tasks"></i>
					<b>Due Tasks</b>
				</p>
			</a>
			<a role="button" class="navbar-burger burger" aria-label="menu" aria-expanded="false" data-target="navbarBasicExample">
				<span aria-hidden="true"></span>
				<span aria-hidden="true"></span>
				<span aria-hidden="true"></span>
			</a>
		</div>
		<div class="navbar-end">
			<div class="navbar-item">
				<div class="buttons">
					<a class="button is-primary is-small" href="{{ path('create_task')}}">
						<strong>Add new task</strong>
					</a>
				</div>
			</div>
		</div>
	</nav>
	{% block content %}{% endblock %}
{% endblock %}
